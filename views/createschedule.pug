extends layout

block content
  .page-header
    h3 Create Schedule
    h2 mimsey creates this schedule

  form.form-horizontal(method='POST')
    .form-group
      label.col-sm-3.control-label(for='test') Test
      .col-sm-7
        input.form-control(type='test', name='test', id='test', value=createschedule.name, placeholder='Test', autofocus, required)
    .form-group
      .col-sm-offset-3.col-sm-7
        button.col-sm-3.btn.btn-primary(type='submit')
          | Submit
